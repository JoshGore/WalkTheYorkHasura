- args:
    sql: CREATE TABLE "public"."point_route"("point_id" integer NOT NULL, "route_id"
      integer NOT NULL, PRIMARY KEY ("point_id","route_id") , FOREIGN KEY ("point_id")
      REFERENCES "public"."points"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN
      KEY ("route_id") REFERENCES "public"."routes"("id") ON UPDATE restrict ON DELETE
      restrict);
  type: run_sql
- args:
    name: point_route
    schema: public
  type: add_existing_table_or_view
