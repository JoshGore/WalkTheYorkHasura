- args:
    sql: CREATE TABLE "public"."lines"("id" serial NOT NULL, "type_id" integer NOT
      NULL, "title" text, "body" text, "geom" geography NOT NULL, PRIMARY KEY ("id")
      , FOREIGN KEY ("type_id") REFERENCES "public"."types"("id") ON UPDATE restrict
      ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: lines
    schema: public
  type: add_existing_table_or_view
