- args:
    sql: CREATE TABLE "public"."users"("id" serial NOT NULL, "username" varchar NOT
      NULL, "password" varchar NOT NULL, "token" varchar NOT NULL, "created_at" timestamptz
      NOT NULL DEFAULT now(), "firstname" varchar NOT NULL, "lastname" varchar NOT
      NULL, PRIMARY KEY ("id") , UNIQUE ("username"));
  type: run_sql
- args:
    name: users
    schema: public
  type: add_existing_table_or_view
