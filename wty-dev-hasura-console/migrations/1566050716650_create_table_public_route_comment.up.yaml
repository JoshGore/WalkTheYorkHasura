- args:
    sql: CREATE TABLE "public"."route_comment"("route_id" integer NOT NULL, "comment_id"
      integer NOT NULL, PRIMARY KEY ("route_id","comment_id") , FOREIGN KEY ("route_id")
      REFERENCES "public"."route"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN
      KEY ("comment_id") REFERENCES "public"."comments"("id") ON UPDATE restrict ON
      DELETE restrict, UNIQUE ("comment_id"));
  type: run_sql
- args:
    name: route_comment
    schema: public
  type: add_existing_table_or_view
